diff --git a/crates/codegen/xai-grok-config/src/shell.rs b/crates/codegen/xai-grok-config/src/shell.rs
--- a/crates/codegen/xai-grok-config/src/shell.rs
+++ b/crates/codegen/xai-grok-config/src/shell.rs
@@ -329,8 +329,10 @@ fn invocation_for(shell: &WindowsShell, command: &str) -> ShellInvocation {
 //      There the user's login shell already lives at the resolved path (e.g. `/run/current-system/sw/bin/bash`, `/opt/homebrew/bin/bash`).
 //   3. `which::which(name)` walks `$PATH`.
 //      Catches NixOS profile shells in `/nix/store/...` or `/etc/profiles/per-user/<u>/bin/` when `/bin/bash` is absent
-//   4. A fixed candidate list: `{/bin, /usr/bin, /usr/local/bin, /opt/homebrew/bin} × {bash,zsh}`.
-//   5. Hardcoded `/bin/<name>`: historical behavior, only reached when every earlier step has failed.
+//   4. On iOS, paths below `.jbroot` beside the running Mach-O. RootHide adds that link to each directory containing a Mach-O, pointing at the randomized jailbreak root.
+//   5. A fixed candidate list: `{/var/jb/bin, /var/jb/usr/bin, /bin, /usr/bin, /usr/local/bin, /opt/homebrew/bin} × {bash,zsh}`.
+//      `/var/jb` is probed first because a rootless bootstrap has no `/bin/sh` at all, and a Rust binary is not libvroot-linked so unprefixed paths stay on rootfs.
+//   6. Hardcoded `/bin/<name>`: historical behavior, only reached when every earlier step has failed.
 //
 // The result is cached per kind in a process-wide `OnceLock`, so the cascade is run at most once per shell kind per process
 
@@ -418,15 +420,39 @@ fn resolve_unix_shell_path(kind: UnixShellKind) -> String {
         return p.to_string_lossy().into_owned();
     }
 
-    // 4) Common install dirs.
-    for dir in ["/bin", "/usr/bin", "/usr/local/bin", "/opt/homebrew/bin"] {
+    // 4) RootHide gives every Mach-O directory a `.jbroot` link to the
+    //    randomized jailbreak root. This Rust binary is not libvroot-linked,
+    //    so it must derive a physical shell path from that link itself.
+    #[cfg(target_os = "ios")]
+    if let Ok(executable) = std::env::current_exe()
+        && let Some(executable_dir) = executable.parent()
+    {
+        for dir in ["bin", "usr/bin"] {
+            let p = executable_dir.join(".jbroot").join(dir).join(name);
+            if is_executable(&p) {
+                return p.to_string_lossy().into_owned();
+            }
+        }
+    }
+
+    // 5) Common install dirs. Probe the rootless prefix first: iOS has no
+    //    /bin/sh on the real rootfs, and this binary does not go through
+    //    libvroot, so `/bin/zsh` from a vroot parent is a lie.
+    for dir in [
+        "/var/jb/bin",
+        "/var/jb/usr/bin",
+        "/bin",
+        "/usr/bin",
+        "/usr/local/bin",
+        "/opt/homebrew/bin",
+    ] {
         let p = std::path::PathBuf::from(dir).join(name);
         if is_executable(&p) {
             return p.to_string_lossy().into_owned();
         }
     }
 
-    // 5) Hardcoded fallback, same as historical behavior. Spawn will fail at runtime on a pure NixOS host with no bash.
+    // 6) Hardcoded fallback, same as historical behavior. Spawn will fail at runtime on a pure NixOS host with no bash.
     kind.hardcoded_default().to_string()
 }
 
