diff --git a/crates/codegen/xai-grok-agent/src/prompt/skills.rs b/crates/codegen/xai-grok-agent/src/prompt/skills.rs
--- a/crates/codegen/xai-grok-agent/src/prompt/skills.rs
+++ b/crates/codegen/xai-grok-agent/src/prompt/skills.rs
@@ -109,6 +109,10 @@ pub async fn list_skills_with_plugins(
     ));
-    skills.extend(collect_injected_skills(
-        &config.bundled_skill_dirs,
-        SkillScope::Bundled,
-    ));
+    // Desktop platform bundles (docx/pptx/xlsx/imagine, …) are not required
+    // for grok as a coding agent on iOS and add open-file load.
+    if !cfg!(target_os = "ios") {
+        skills.extend(collect_injected_skills(
+            &config.bundled_skill_dirs,
+            SkillScope::Bundled,
+        ));
+    }
 
@@ -307,9 +311,11 @@ async fn list_skills_with_options(
 
-    let bundled_dir = global_dir.join("bundled");
-    collect_discovered_paths(
-        find_skill_paths(&bundled_dir),
-        SkillScope::Bundled,
-        &mut seen_canonical_paths,
-        &mut skill_files,
-    );
+    if !cfg!(target_os = "ios") {
+        let bundled_dir = global_dir.join("bundled");
+        collect_discovered_paths(
+            find_skill_paths(&bundled_dir),
+            SkillScope::Bundled,
+            &mut seen_canonical_paths,
+            &mut skill_files,
+        );
+    }
 
diff --git a/crates/codegen/xai-grok-shell/src/agent/mvp_agent/mod.rs b/crates/codegen/xai-grok-shell/src/agent/mvp_agent/mod.rs
--- a/crates/codegen/xai-grok-shell/src/agent/mvp_agent/mod.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/mvp_agent/mod.rs
@@ -2006,2 +2006,5 @@ impl MvpAgent {
     pub(crate) fn maybe_sync_bundle_in_background(&self, force: bool) {
+        if cfg!(target_os = "ios") {
+            return;
+        }
         use crate::extensions::bundle::{
diff --git a/crates/codegen/xai-grok-shell/src/config/watcher.rs b/crates/codegen/xai-grok-shell/src/config/watcher.rs
--- a/crates/codegen/xai-grok-shell/src/config/watcher.rs
+++ b/crates/codegen/xai-grok-shell/src/config/watcher.rs
@@ -579,1 +579,8 @@ impl SkillsFileWatcher {
+        let mut compat = xai_grok_tools::types::compat::CompatConfig::default();
+        // iOS skips Claude/Cursor watches so vendor skill trees cannot add
+        // open-file load; `.grok` / `.agents` / $GROK_HOME stay.
+        if cfg!(target_os = "ios") {
+            compat.claude.skills = false;
+            compat.cursor.skills = false;
+        }
         let dirs_to_watch = xai_grok_agent::prompt::skills::collect_skill_config_dirs(
@@ -583,3 +590,3 @@ impl SkillsFileWatcher {
             config_paths,
-            xai_grok_tools::types::compat::CompatConfig::default(),
+            compat,
         );
diff --git a/crates/codegen/xai-grok-shell/src/extensions/bundle.rs b/crates/codegen/xai-grok-shell/src/extensions/bundle.rs
--- a/crates/codegen/xai-grok-shell/src/extensions/bundle.rs
+++ b/crates/codegen/xai-grok-shell/src/extensions/bundle.rs
@@ -189,2 +189,5 @@ pub(crate) async fn sync_bundle_to_root(
 ) -> anyhow::Result<BundleSyncResult> {
+    if cfg!(target_os = "ios") {
+        anyhow::bail!("platform bundled skills are not installed on iOS");
+    }
     if !has_bundle_credentials(auth_manager, deployment_key) {
